This add-on will create user account if someone tries to log in with the email not found in the system.

If self-activation is required in membership preferences, then the user will be send email with the password and email verification link will also be included in the message.